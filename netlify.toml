[build]
  command = "npx prisma db push && prisma generate && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  DATABASE_URL = "file:/tmp/dev.db"
  PRISMA_CLI_QUERY_ENGINE_TYPE = "binary"
